# pipreqs-auto
 pipreqs-auto is a Python automation script that intelligently manages project dependencies. It extracts required packages, resolves sub-dependencies, and optionally installs them—all in a single execution.
